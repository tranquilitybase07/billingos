import * as react_jsx_runtime from 'react/jsx-runtime';

interface PaginatorProps {
    totalCount: number;
    pageSize: number;
    currentPage: number;
    siblingCount?: number;
    onPageChange: (page: number) => void;
    className?: string;
    currentURL: URLSearchParams;
}
declare const Paginator: ({ totalCount, pageSize, currentPage, siblingCount, onPageChange, className, currentURL, }: PaginatorProps) => react_jsx_runtime.JSX.Element | null;

declare const usePagination: ({ totalCount, pageSize, siblingCount, currentPage, }: {
    totalCount: number;
    pageSize: number;
    siblingCount?: number;
    currentPage: number;
}) => (number | symbol)[] | undefined;

export { Paginator as default, usePagination };
