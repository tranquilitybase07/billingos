import * as react_jsx_runtime from 'react/jsx-runtime';
import * as React from 'react';

interface ComboboxProps<T> {
    items: T[];
    value: string | null;
    selectedItem: T | null;
    onChange: (value: string | null) => void;
    onQueryChange: (query: string) => void;
    getItemValue: (item: T) => string;
    getItemLabel: (item: T) => string;
    renderItem?: (item: T) => React.ReactNode;
    isLoading?: boolean;
    placeholder?: string;
    searchPlaceholder?: string;
    emptyLabel?: string;
    className?: string;
}
declare function Combobox<T>({ items, value, selectedItem, onChange, onQueryChange, getItemValue, getItemLabel, renderItem, isLoading, placeholder, searchPlaceholder, emptyLabel, className, }: ComboboxProps<T>): react_jsx_runtime.JSX.Element;

export { Combobox, type ComboboxProps };
