import * as react_jsx_runtime from 'react/jsx-runtime';
import { ColumnDef, PaginationState, SortingState, OnChangeFn, Cell, Row, RowSelectionState } from '@tanstack/react-table';

interface ReactQueryLoading {
    isFetching: boolean;
    isFetched: boolean;
    isLoading: boolean;
    status: string;
    fetchStatus: string;
}
interface DataTableProps<TData, TValue> {
    columns: ColumnDef<TData, TValue>[];
    data: TData[];
    rowCount?: number;
    pageCount?: number;
    pagination?: PaginationState;
    onPaginationChange?: OnChangeFn<PaginationState>;
    sorting?: SortingState;
    onSortingChange?: OnChangeFn<SortingState>;
    getSubRows?: (row: TData) => TData[] | undefined;
    className?: string;
    wrapperClassName?: string;
    headerClassName?: string;
    isLoading: boolean | ReactQueryLoading;
    getCellColSpan?: (cell: Cell<TData, unknown>) => number;
    getRowId?: (originalRow: TData, index: number, parent?: Row<TData>) => string;
    rowSelection?: RowSelectionState;
    enableRowSelection?: boolean;
    onRowSelectionChange?: OnChangeFn<RowSelectionState>;
    onRowClick?: (row: Row<TData>) => void;
}
type DataTableColumnDef<TData, TValue = unknown> = ColumnDef<TData, TValue>;
type DataTablePaginationState = PaginationState;
type DataTableSortingState = SortingState;
declare function DataTable<TData, TValue>({ columns, data, rowCount, pageCount, pagination, onPaginationChange, sorting, onSortingChange, getSubRows, className, wrapperClassName, headerClassName, isLoading, getCellColSpan, getRowId, rowSelection, enableRowSelection, onRowSelectionChange, onRowClick, }: DataTableProps<TData, TValue>): react_jsx_runtime.JSX.Element;

export { DataTable, type DataTableColumnDef, type DataTablePaginationState, type DataTableSortingState, type ReactQueryLoading };
