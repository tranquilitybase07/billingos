import * as react_jsx_runtime from 'react/jsx-runtime';
import { ButtonProps } from '../ui/button.mjs';
import React__default from 'react';
import 'class-variance-authority/types';
import 'class-variance-authority';

declare const Button: {
    ({ ref, className, wrapperClassNames, variant, size, loading, fullWidth, disabled, children, type, ...props }: ButtonProps & {
        ref?: React__default.RefObject<HTMLButtonElement>;
        wrapperClassNames?: string;
        loading?: boolean;
        fullWidth?: boolean;
    }): react_jsx_runtime.JSX.Element;
    displayName: string;
};

declare const RawButton: {
    ({ ref, className, variant, size, children, ...props }: ButtonProps & {
        ref?: React__default.RefObject<HTMLButtonElement>;
    }): react_jsx_runtime.JSX.Element;
    displayName: string;
};

export { ButtonProps, RawButton, Button as default };
